{
  "name": "travel-project",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"yarn workspace client dev:client\" \"yarn workspace server dev:server\"",
    "dev:db": "docker compose --env-file ./server/.env up"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.2.0",
    "@typescript-eslint/parser": "^7.2.0",
    "concurrently": "8.2.2",
    "eslint": "^8.57.0",
    "typescript": "^5.2.2"
  }
}
